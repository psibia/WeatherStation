<Window x:Class="WeatherStation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WeatherStation"
        mc:Ignorable="d"
        Title="Погодная станция" Height="200" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Температура:"/>
        <TextBlock Grid.Row="0" Text="{Binding Temperature}" Margin="100,0,0,0"/>

        <TextBlock Grid.Row="1" Text="Влажность:"/>
        <TextBlock Grid.Row="1" Text="{Binding Humidity}" Margin="100,0,0,0"/>

        <Button Grid.Row="2" Content="Отправить в телеграм" Command="{Binding SendTemperatureCommand}" Margin="0,10,0,0"/>
    </Grid>
</Window>
